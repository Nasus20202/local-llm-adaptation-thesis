# Project Status

- **Current milestone:** M1 — Benchmark Dataset and Evaluation Foundation
- **Current research objective:** Define and human-review the protected development evaluator contract for the 24 approved Issue #35 scenario inputs while preserving semantic fairness, frozen Kubernetes v1.36.4 evidence, protected custody, and the final-test boundary.
- **Canonical OpenSpec specifications:** The merged foundation remains under `openspec/specs/`. The current planning delta is `openspec/changes/development-protected-evaluator-contract`, which extends only `evaluation-protocol` and `benchmark-pilot-dataset`; it is not yet approved or implemented.
- **Specification status:** The Issue #2 benchmark-domain policy and Issue #4 pilot/evaluator methodology remain accepted. PR #44 exposed a concrete implementation gap: the canonical specs cover evaluator identity, generic fixtures, rubric calibration, and protected separation, but do not yet define the per-family semantic-contract interface, hierarchical deterministic/judge/human assessment, class-specific score derivation, source-evidence binding, qualified-judge controls, and anti-copying qualification required for these approved inputs. This Work change fills only that gap.
- **Implementation status:** The existing pilot/evaluation validation foundation is on `main`. No protected development evaluator implementation or protected Kubernetes answer payload has been added. Codex implementation remains closed until Human Gate A approves the active OpenSpec package. Gate A would authorize generic model-agnostic semantic-assessment/qualification records only; it would not authorize selecting or running a concrete LLM judge.
- **Independent review status:** PR #33 passed independent review before merge. PR #38 was squash-merged as `67c2b34ab7d725b170568be4ac57bed72ad57b3c`; PR #40 froze source/rights as `d5667bba09ed12ed71441511c72f6219d76fd25d`; PR #42 froze pre-authoring custody/metadata as `575fc4df4597c6b13ad3535e34474ddc455b4527`; and the human-approved 24 development-only scenario inputs were squash-merged in PR #44 as `a4e377a189fbd5ba74fdd143c7bcea9ad9458141`.
- **Experiment status:** Not started; no pilot or formal participant-model execution and no experimental results exist.
- **Current gate:** Human Gate A for `development-protected-evaluator-contract`. The 24 model-facing questions, source/release selection, condition/comparator rules, metric families, and custody roots are already frozen and unchanged. Protected answer-bearing contracts/fixtures, concrete judge selection/execution, participant-model execution, training, final-test work, real `kind`, live W1, semantic-pair selection, and C2 selection remain closed.
- **Next action:** Review the protected evaluator design and OpenSpec delta. Approval authorizes Codex to implement typed protected-contract/custody records, deterministic score derivation, a model-agnostic semantic-assessor hierarchy, qualified-judge configuration/qualification records, human adjudication/audit routing, source-binding validation, copying-neutral qualification, and synthetic tests. After that implementation is reviewed/merged, protected evaluator bundles are authored/frozen; then a separate human-approved judge freeze selects the exact judge model/backend/prompt/configuration, human-labelled qualification fixtures, acceptance thresholds, and audit policy before any judge-model run.

This file records the current research and development stage. Toolchain versions belong in project configuration and reproducibility records, not here.
